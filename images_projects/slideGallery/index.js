let slideIndex = 1;

// previues button
function plusSlides(n){
    showSlides(slideIndex =slideIndex + n);
    // showSlides(slideIndex += n);
}
// image control
function controlImg(n){
    showSlides(slideIndex = n);
}



